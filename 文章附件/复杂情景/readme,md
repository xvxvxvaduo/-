本次实验的物理情景与文章中描述的一致，唯一的区别只有跨度的不同。

本次次实验使用全连接神经网络架构，预实验后，我们决定对于小跨度和中跨度建立有七个隐藏层的网络，大跨度建立有21个个隐藏层的网络，每个隐藏层50个节点，使用ReLu激活函数。样本批次大小为32,学习率为0.001。

命名为小，中，大分别对应跨度为dt=2.073e-8，dt=4.176e-8，dt=2.073e-7。后缀带有数字是同一个跨度下的不同图片，标记有up的为使用保能量网络预测的结果，未标记的则是纯数据驱动网络的预测结果。

图的含义已在文章中介绍过。

